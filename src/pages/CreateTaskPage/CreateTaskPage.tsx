import { Typography, Box } from "@mui/material";

const CreateTaskPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Создание задачи</Typography>
    </Box>
  );
}

export default CreateTaskPage;
