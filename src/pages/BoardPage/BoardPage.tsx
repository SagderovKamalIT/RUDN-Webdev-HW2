import { Typography, Box } from "@mui/material";

const BoardPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Kanban-доска</Typography>
    </Box>
  );
}

export default BoardPage;
